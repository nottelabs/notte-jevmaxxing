export const metadata = { title: "Wikipedia Race: Jev vs Cerebras | Notte", description: "Two AI models race through Wikipedia using real Notte browsers. Same start, same target, only article links." };

export default function RaceLayout({ children }: { children: React.ReactNode }) {
  return children;
}
